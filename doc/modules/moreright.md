# moreright 模块

## 模块定位

`src/moreright` 是一个非常小的 feature 模块，当前快照只暴露了一个 hook：`useMoreRight.tsx`。

规模统计：

- 文件数：1
- 行数：26
- 核心文件：`moreright/useMoreRight.tsx`

## 当前可见职责

从命名推断，它是一个很窄的 UI/实验功能适配层，可能和某种面板、布局或实验性更右侧视图有关。

由于当前快照只剩下一个极小文件，因此能确认的事实只有：

- 它不是主链路模块
- 它是被 feature 化隔离的小型 UI 能力

## 为什么仍值得保留文档

像这种目录非常容易在代码阅读时被误判成“可以忽略的噪音”，但它们通常承担两类责任：

- 实验功能开关的承接点
- 历史功能残留或未来能力预留位

## 维护建议

- 若未来该模块继续发展，建议在文档中补充更清晰的产品语义。
- 如果长期保持单文件极小状态，也可以考虑合并进更贴近调用点的模块，减少目录噪音。
